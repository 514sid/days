type DurationUnits = "years" | "quarters" | "months" | "weeks" | "days" | "hours" | "minutes" | "seconds";

export const DurationUnit: Record<string, DurationUnits> = {
    Years  : "years",
    Months : "months",
    Weeks  : "weeks",
    Days   : "days",
    Hours  : "hours",
    Minutes: "minutes",
    Seconds: "seconds"
}